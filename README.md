## Project Overview

This project converts YouTube videos into infographics using an LLM-powered pipeline. It fetches the video transcript, extracts and structures the key points, generates an infographic layout in JSON, and renders the final visual output automatically. The system is built with LangGraph and MCP-based tools for transcript retrieval and infographic rendering.
